<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="atividade.mobile.tatiana.trabalhocontrolelivros">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.menu.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.form.NewUserActivity" />
        <activity android:name=".activities.alteration.ForgottenPasswordActivity" />
        <activity android:name=".activities.alteration.ChangePasswordActivity" />
        <activity
            android:name=".activities.menu.ListActivity"
            android:label="@string/title_activity_list" />
        <activity android:name=".activities.form.BookFormActivity" />
        <activity android:name=".activities.form.ExemplarFormActivity" />
        <activity android:name=".activities.menu.UserMenuActivity" />
        <activity android:name=".activities.menu.MainMenuActivity" />
        <activity android:name=".activities.menu.SettingsActivity" />
        <activity android:name=".activities.view.ExemplarViewActivity" />
        <activity android:name=".activities.view.ViewObjectActivity" />
        <activity android:name=".activities.alteration.AlterAvatarActivity" />
        <activity android:name=".activities.menu.NewItemMenuActivity" />
        <activity android:name=".activities.form.StandardFormActivity" />
        <activity android:name=".activities.view.BookViewActivity" />
        <activity android:name=".activities.alteration.ChangeStatusActivity" />
        <activity android:name=".activities.menu.SearchActivity" />
        <activity android:name=".activities.view.SearchResultActivity" />
        <activity android:name=".activities.view.ShowExemplarCoverActivity" />
        <activity android:name=".activities.alteration.AddAuthorRelationshipActivity" />
        <activity android:name=".activities.alteration.AddSeriesRelationshipActivity" />
        <activity android:name=".activities.alteration.RemoveAuthorRelationshipActivity" />
        <activity android:name=".activities.alteration.RemoveSeriesRelationshipActivity" />
        <activity android:name=".activities.alteration.EditCommentExemplarActivity" />
        <activity android:name=".activities.alteration.GiveNewClassificationActivity"></activity>
    </application>

</manifest>